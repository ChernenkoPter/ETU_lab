from entropy import entropy_2


def partition_entropy(partition, words_to_counts):
    result = 0
    distribution = list(x[1] for x in words_to_counts)
    # TODO реализовать вычисление средней длины кодового слова по размеру групп и количеству символов в алфавите
    return result
